apiVersion: apps/v1
kind: Deployment
metadata:
  name: iss-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iss-frontend
  template:
    metadata:
      labels:
        app: iss-frontend
    spec:
      containers:
      - name: frontend
        image: 22i0838/iss-frontend:83211f2f8a212f1cb26bf90f55df56bc1f6a81ae
        imagePullPolicy: Always
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  selector:
    app: iss-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80