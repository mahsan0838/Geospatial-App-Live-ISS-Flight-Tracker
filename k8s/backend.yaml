apiVersion: apps/v1
kind: Deployment
metadata:
  name: iss-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: iss-backend
  template:
    metadata:
      labels:
        app: iss-backend
    spec:
      containers:
      - name: backend
        image: 22i0838/iss-backend:8585215c8d50f5a6ae56e3c932e4a2dbd2fa12c1
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: LoadBalancer
  selector:
    app: iss-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000